
def sum(): #매개변수 및 반환값 없다.
    num1 = eval(input("숫자1 입력 : "))
    num2 = eval(input("숫자2 입력 : "))
    print("합 : %d" %(num1+num2))
    return num1+num2
    # 저장하고 출력하고 종료되는 함수는 반환값을 만들지 않는다.
    # 함수에서 생성된 데이터를 함수밖에서 이용해서 프로그램 진행해야 할 때 리턴 값을 만들어야 한다.
# 리턴문을 만나면 함수는 종료된다.
# 리턴문은 함수 마지막에 위치해야한다.




sum()
sum()
